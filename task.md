# Vercel Deployment Tasks

- [x] Create `vercel.json` at project root
- [x] Modify `server/config/db.js` — add connection caching
- [x] Modify `server/server.js` — remove app.listen(), export app
- [x] Modify `client/vite.config.js` — explicit outDir + dev proxy
- [x] Update `client/.env` — set VITE_API_URL to /api
